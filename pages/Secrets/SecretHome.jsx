import React from "react"

//import { requireAuth } from "../../utils";

// export async function loader({ request }) {
//   await requireAuth(request);
//   return {}
// }

export default function SecretHome() {
  return (
    <div>
      <h1>I'm the secret landing page!</h1>
    </div>
  )
}
